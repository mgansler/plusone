version: '3'

services:
  web:
    image: ${APP_SUITE_DOCKER_IMAGE}
    labels:
      - 'traefik.http.routers.git.rule=Host(`suite.martingansler.de`)'
      - 'traefik.http.routers.git.tls=true'
      - 'traefik.http.routers.git.tls.certresolver=letsencrypt'
