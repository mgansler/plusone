stages:
  - prepare
  - test
  - build
  - package
  - deploy

cache:
  key:
    files:
      - yarn.lock
  paths:
    - node_modules/

include:
  - /apps/feeds/.gitlab-ci.yml
  - /apps/suite/.gitlab-ci.yml

lint:
  stage: test
  tags:
    - docker
  script:
    - yarn nx run-many --target=lint --all
