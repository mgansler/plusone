version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - yarn install
        build:
          commands:
            - yarn nx build github-pipeline-status
      artifacts:
        baseDirectory: ../../dist/apps/github-pipeline-status
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: apps/github-pipeline-status
