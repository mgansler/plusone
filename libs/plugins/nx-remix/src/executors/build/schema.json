{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "title": "Remix build executor",
  "description": "",
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "The output path of the generated files."
    }
  },
  "required": [
    "outputPath"
  ]
}
