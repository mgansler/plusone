import type { Tree } from '@nx/devkit'

// This is just a mock
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function addDependencies(tree: Tree) {
  // we do not want to install dependencies in unit tests
}
