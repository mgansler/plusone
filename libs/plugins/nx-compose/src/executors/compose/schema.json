{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Compose executor",
  "description": "",
  "type": "object",
  "properties": {
    "targets": {
      "type": "array",
      "default": [],
      "description": "List of targets to execute",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["targets"]
}
