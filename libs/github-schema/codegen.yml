overwrite: true
schema: "https://docs.github.com/public/schema.docs.graphql"

generates:
  libs/github-schema/src/lib/github-schema.ts:
    plugins:
      - typescript
    config:
      skipTypename: false
