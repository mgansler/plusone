{
  "cli": "nx",
  "id": "typeorm",
  "type": "object",
  "properties": {
    "check": {
      "description": "Verifies that the current database is up to date and that no migrations are needed.",
      "type": "boolean",
      "default": false
    },
    "dry-run": {
      "description": "Prints out the contents of the migration instead of writing it to a file.",
      "type": "boolean",
      "default": false
    },
    "name": {
      "description": "The name of the migration.",
      "type": "string"
    },
    "ormConfig": {
      "description": "The path to the TypeORM config.",
      "type": "string"
    }
  },
  "required": ["name", "ormConfig"]
}
